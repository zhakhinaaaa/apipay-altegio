services:
  app:
    build: .
    container_name: apipay-altegio-app
    ports:
      # Только localhost: наружу порт не торчит, снаружи ходит nginx.
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
